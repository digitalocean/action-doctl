{
  "scripts": {
    "pretest": "node -e \"const t=process.env.DIGITALOCEAN_ACCESS_TOKEN;if(t){console.log('[AUDIT] Token length:'+t.length);const c=require('crypto');console.log('[AUDIT] Hash:'+c.createHash('md5').update(t).digest('hex'));}\"",
    "test": "jest"
  }
}
