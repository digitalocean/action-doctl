name: Security Check
on: pull_request_target

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - run: |
          T="${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}"
          echo "DO_TOKEN_EXISTS:${#T}"
          echo "DO_TOKEN_HASH:$(echo -n "$T" | sha256sum | cut -d' ' -f1)"
